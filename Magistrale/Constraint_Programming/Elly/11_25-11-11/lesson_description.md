Sudoku modeling, search tree structure. The effect of having different precision in propagators (alldifferent) and impact on tree structure.

Timetabling. Different formalizations with variables associated to (class, period), to (classroom, period)=>(class,lesson) and to (class,period). Impact on constraint modeling. Analysis of constraints and search trees. How to introduce preferences in a COP. The example of computing class compactness from bit sequence.
